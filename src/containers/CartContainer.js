import React, { Component } from 'react';
import { connect } from 'react-redux';
import Cart from '../components/cart';

export default class CartContainer extends Component {
    render() {
        return (
            <h4> Cart CartContainer</h4>
        );
    }
}